{
    "name": "wishbook-root",
    "version": "1.0.1",
    "scripts": {
        "build": "cd frontend && npm install && npm run build"
    },
    "private": true,
    "engines": {
        "node": "20.x"
    }
}