{
    "name": "wishbook-root",
    "version": "1.0.1",
    "scripts": {
        "build": "cd frontend && npm install && npm run build && cp -r .next ../.next && cp -r public ../public"
    },
    "private": true,
    "engines": {
        "node": "20.x"
    },
    "devDependencies": {
        "next": "^14.0.0"
    }
}